export interface TypeModel {
  id: number;
  name: string;
  createdDate?: Date;
  updatedDate?: Date;
}

export interface ListTypeModel {
  id: number;
  name: string;
  createdDate?: Date;
  updatedDate?: Date;
}

export interface ListTypeModelRef {
  id: number;
}
